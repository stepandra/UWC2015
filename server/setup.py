from setuptools import setup

setup(
    name="Volunteers",
    version="0.1",
    install_requires=[
        "Flask",
        "flask-bcrypt",
        "flask-login",
        "peewee",
        "Flask-WTF",
    ]
)